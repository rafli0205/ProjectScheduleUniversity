<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme (dark) -->
    <style name="Base.Theme.ScheduleU" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Kalau mau warna khusus mode gelap, atur di sini -->
        <!-- <item name="colorPrimary">@color/purple_splash</item> -->
    </style>

    <!-- Theme utama aplikasi (dark) -->
    <style name="Theme.ScheduleU" parent="Base.Theme.ScheduleU" />

    <!-- Theme khusus Splash Screen (dark) -->
    <style name="Theme.ScheduleU.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/purple_splash</item>
        <item name="android:statusBarColor">@color/purple_splash</item>
    </style>

</resources>
